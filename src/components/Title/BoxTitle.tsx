const BoxTitle = ({ title }) => {
    return (
        <div className="box-title dark:text-slate-100 text-slate-900">
            <span className="dark:text-slate-100 text-slate-900">{title}</span>
        </div>
    );
}

export default BoxTitle;