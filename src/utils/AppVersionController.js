// For switch between different version in each part of application
// Empty value means version 1
// For define version use "v" + "number" + "/", like v2/ , v3/ and etc.
const SECTIONS_VERSION = {
    VIRTUAL_CLOUD_SERVER: {
        LIST: '',
        SETUP: '',
        DETAILS: 'v2/'
    }
}

export default SECTIONS_VERSION;